# Artifacts

Identity and provenance for `{{BRANCHNAME}}`.

The birth certificate and anything else that establishes who this branch is or
what milestone it reached.

Never working output: test scripts, dispatch logs, run logs and delivery briefs
to other branches belong in `docs.local/`, `dropbox/`, or a test-output location —
none of them represent ownership or identity.
